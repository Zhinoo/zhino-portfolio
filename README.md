## 🚀 Getting Started

To view or deploy this portfolio locally, follow these simple steps:

1.  **Clone the Repository:**
    ```bash
    git clone https://github.com/Zhinoo/zhino-portfolio
    ```

2.  **Navigate to the Directory:**
    ```bash
    cd zhino-portfolio
    ```

3.  **Launch the Site:**
    Open the **`index.html`** file directly in your web browser. Since this is a **static site** (no backend server required), it will render immediately.